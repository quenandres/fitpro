services:

  app:
    build: .
    container_name: fitpro-app
    ports:
      - "5174:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
      NODE_ENV: development
    restart: unless-stopped  

