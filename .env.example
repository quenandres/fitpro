# URL base del gateway (gym-gateway). Auth, CRUD e IA rutinas (/api/ai/routine).
# Docker publica 8008→8000; uvicorn --reload local queda en 8000.
# Docker compose expone gateway en localhost:8008
VITE_GATEWAY_URL=http://localhost:8008
